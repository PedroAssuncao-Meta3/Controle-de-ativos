@model Controle_Ativos.ViewModel.AtributoXTipoPatrimonioViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>AtributoXTipoPatrimonio</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group">
                <label asp-for="AtributoId" class="control-label"></label>
                <select asp-for="AtributoId" class="form-control" asp-items="@(new SelectList(Model.Atributos, "Id", "Descricao"))">
                    <option></option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="TipoPatrimonioId" class="control-label"></label>
                <select asp-for="TipoPatrimonioId" class="form-control" asp-items="@(new SelectList(Model.TipoPatrimonios, "Id", "Descricao"))">
                    <option></option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
