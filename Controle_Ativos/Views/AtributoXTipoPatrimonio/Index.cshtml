@model IEnumerable<Controle_Ativos.ViewModel.AtributoXTipoPatrimonioViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Atributo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoPatrimonio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Atributo.Descricao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPatrimonio.Descricao)
            </td>
            <td>
                <a class="button" asp-action="Edit" asp-route-id="@item.Id">
                    <i class="material-icons">edit</i>
                </a>
                <a class="button" asp-action="Details" asp-route-id="@item.Id">
                    <i class="material-icons">plagiarism</i>
                </a>
                <a class="button" asp-action="Delete" asp-route-id="@item.Id">
                    <i class="material-icons">delete</i>
                </a>
            </td>
        </tr>
}
    </tbody>
</table>
